import { STATISTICS_SCHEMA } from "@/src/utils/schemas/members";
import React from "react";

export default function StatsFeed({ stats }: { stats: STATISTICS_SCHEMA[] }) {
  return <div>StatsFeed</div>;
}
